apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'serializacion.Main'

jar {
  manifest {
    attributes(
      'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
      'Main-Class': 'serializacion.Main'
    )
  }
}
