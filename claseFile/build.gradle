apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'claseFile.Main'

jar {
  manifest {
    attributes(
      'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
      'Main-Class': 'claseFile.Main'
    )
  }
}
