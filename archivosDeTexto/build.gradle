apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'archivosDeTexto.Main'

jar {
  manifest {
    attributes(
      'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
      'Main-Class': 'archivosDeTexto.Main'
    )
  }
}
